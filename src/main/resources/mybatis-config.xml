<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- mybatis的全局配置文件 -->
<!-- 配置完spring后可删除 -->
<configuration>
	<!-- 
	<settings>
		<setting name="lazyLoadingEnabled" value="true"></setting>
		<setting name="aggressiveLazyLoading" value="false" />
		<setting name="lazyLoadTriggerMethds" value="clone" />
	</settings>
	 -->
	 
	<!-- mybatis懒加载 -->
	<!-- 1启用延迟加载特性，不配置默认关闭该特性 -->
	<!-- 1查询时，关闭关联对象即时加载以提高性能 -->
	<!-- 2按需加载: false:使用关联属性，及时加载; true,加载对象，则加载所有属性， -->
	<!-- 2mybatis执行完sql语句，组装返回的对象的时候，按sql指定的字段来加载 -->
	<!-- 3自动将equals,clone,hashCode,toString这几个方法定义为延迟加载的加载触发方法 -->
	
	<!-- 
		0:属性配置（延迟加载，别名）
		1:创建连接池
		2:创建事务管理器
		3:关联Mapper文件
	 -->

</configuration>